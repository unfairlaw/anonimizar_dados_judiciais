# Lambda-specific requirements
# Note: Some packages need to be in Lambda layers due to size constraints

# Core (include in deployment package)
boto3>=1.28.0
botocore>=1.31.0

# These should be in Lambda layers:
# langchain>=0.1.0
# langchain-community>=0.1.0
# langchain-openai>=0.0.8
# chromadb>=0.4.22
# sentence-transformers>=2.3.0
# rank-bm25>=0.2.2
